#include <iostream>
#include <cstring>
#include <algorithm>

using i64 = long long;

int n;
int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(0); std::cout.tie(0);

	std::cin >> n;

	

	return 0;
}